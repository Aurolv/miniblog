<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Miniblog" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="layout-body">
    <div class="background-gradient"></div>

    <header class="site-header">
      <nav class="nav-container">
        <div class="nav-left">
          <%= link_to "Miniblog", root_path, class: "brand-link" %>
          <%= link_to "Posts", posts_path, class: "nav-link" %>
          <% if logged_in? %>
            <%= link_to "Drafts", drafts_posts_path, class: "nav-link" %>
            <%= link_to "New post", new_post_path, class: "nav-link-active" %>
          <% end %>
        </div>

        <div class="nav-right">
          <% if logged_in? %>
            <span class="user-email"><%= current_user.email %></span>
            <%= button_to "Log out", logout_path, method: :delete, class: "btn-logout" %>
          <% else %>
            <%= link_to "Log in", login_path, class: "btn-login" %>
          <% end %>
        </div>
      </nav>
    </header>

    <div class="flash-container">
      <% flash.each do |k,v| %>
        <% classes = k.to_s == "notice" ? "flash-notice" : "flash-alert" %>
        <div class="flash-message <%= classes %>">
          <div><%= v %></div>
          <button class="flash-close" onclick="this.parentElement.remove()">✕</button>
        </div>
      <% end %>
    </div>

    <main class="main-content">
      <%= yield %>
    </main>

    <footer class="site-footer">
      <div class="footer-container">
        <span>© <%= Time.current.year %> Miniblog</span>
        <span>Built with Rails • Hotwire • Tailwind</span>
      </div>
    </footer>
  </body>
</html>
