<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Miniblog" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen grid bg-slate-50">
    <div class="fixed inset-0 -z-10 bg-gradient-to-tr from-fuchsia-300 via-indigo-300 to-cyan-200"></div>



    <header class="fixed top-0 inset-x-0 z-50 bg-white/80 backdrop-blur-md border-b border-slate-200">
      <nav class="mx-auto max-w-5xl h-14 px-4 sm:px-6 lg:px-8 flex items-center justify-between">
        <div class="flex items-center gap-4">
          <%= link_to "Miniblog", root_path, class: "text-2xl font-semibold tracking-tight text-slate-900 hover:text-indigo-600" %>
          <%= link_to "Posts", posts_path, class: "text-lg text-slate-600 hover:text-slate-900 relative after:absolute after:left-0 after:-bottom-1 after:h-0.5 after:w-0 hover:after:w-full after:bg-indigo-600 after:transition-all" %>
          <% if logged_in? %>
            <%= link_to "Drafts", drafts_posts_path, class: "text-lg text-slate-600 hover:text-slate-900 relative after:absolute after:left-0 after:-bottom-1 after:h-0.5 after:w-0 hover:after:w-full after:bg-indigo-600 after:transition-all" %>
            <%= link_to "New post", new_post_path, class: "text-lg font-medium text-indigo-700 relative hover:text-indigo-800 after:absolute after:left-0 after:-bottom-1 after:h-0.5 after:w-0 hover:after:w-full after:bg-indigo-600 after:transition-all" %>
          <% end %>
        </div>

        <div class="flex items-center gap-3">
          <% if logged_in? %>
            <span class="hidden sm:inline text-sm text-slate-600"><%= current_user.email %></span>
            <%= button_to "Log out", logout_path, method: :delete,
                  class: "inline-flex items-center rounded-xl bg-slate-900 px-3 py-1.5 text-sm font-medium text-white hover:bg-slate-800 transition" %>
          <% else %>
            <%= link_to "Log in", login_path,
                  class: "inline-flex items-center rounded-xl bg-indigo-600 px-3 py-1.5 text-sm font-medium text-white hover:bg-indigo-700 transition" %>
          <% end %>
        </div>
      </nav>
    </header>

    <!-- Flash -->
    <div class="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8 mt-16">
      <% flash.each do |k,v| %>
        <% classes = k.to_s == "notice" ? "bg-emerald-100 text-emerald-800 border-emerald-200" : "bg-rose-50 text-rose-800 border-rose-200" %>
        <div class="mt-3 border rounded-xl px-4 py-2 text-sm flex items-start justify-between <%= classes %>">
          <div><%= v %></div>
          <button class="ml-4 text-slate-500 hover:text-slate-700" onclick="this.parentElement.remove()">✕</button>
        </div>
      <% end %>
    </div>

    <!-- Main -->
    <main class="mx-56 px-4 sm:px-6 lg:px-8 py-8">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="border-t border-slate-200 bg-white/60 backdrop-blur">
      <div class="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8 h-14 flex items-center justify-between text-xs text-slate-500">
        <span>© <%= Time.current.year %> Miniblog</span>
        <span>Built with Rails • Hotwire • Tailwind</span>
      </div>
    </footer>
  </body>

</html>
